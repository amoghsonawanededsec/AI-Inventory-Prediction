import { FormEvent, useEffect, useState } from 'react'
import { Plus, Search } from 'lucide-react'
import { api } from '../lib/api'
import type { Product } from '../types'
import { StatusBadge } from '../components/StatusBadge'

type Category = { id: number; name: string }; type Supplier = { id: number; name: string }
export default function ProductsPage() {
  const [products, setProducts] = useState<Product[]>([]), [categories, setCategories] = useState<Category[]>([]), [suppliers, setSuppliers] = useState<Supplier[]>([]), [query, setQuery] = useState(''), [showForm, setShowForm] = useState(false), [error, setError] = useState('')
  const load = () => api<{items: Product[]}>('/api/products?page_size=100').then(r => setProducts(r.items)).catch(e => setError(e.message))
  useEffect(() => { load(); api<Category[]>('/api/categories').then(setCategories); api<Supplier[]>('/api/suppliers').then(setSuppliers) }, [])
  async function create(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const body = { sku: String(form.get('sku')), name: String(form.get('name')), category_id: Number(form.get('category')), supplier_id: Number(form.get('supplier')), price: Number(form.get('price')), current_stock: Number(form.get('stock')), minimum_stock: Number(form.get('minimum')), maximum_stock: Number(form.get('maximum')), reorder_point: Number(form.get('reorder')), safety_stock: Number(form.get('safety')), lead_time_days: Number(form.get('lead')), unit: 'pack', status: 'active' }; try { await api('/api/products', { method: 'POST', body: JSON.stringify(body) }); setShowForm(false); load() } catch (e) { setError(e instanceof Error ? e.message : 'Could not create product') } }
  const shown = products.filter(p => `${p.name} ${p.sku} ${p.category_name}`.toLowerCase().includes(query.toLowerCase()))
  return <><header className="page-header"><div><p className="eyebrow">CATALOG</p><h1>Products & inventory</h1><p>Manage products, stock thresholds and shelf-life information.</p></div><button className="primary-button" onClick={() => setShowForm(!showForm)}><Plus size={17}/>Add product</button></header>{showForm && <form onSubmit={create} className="panel product-form"><h2>New product</h2><div className="form-grid"><label>SKU<input name="sku" required placeholder="INV-031"/></label><label>Name<input name="name" required/></label><label>Category<select name="category" required>{categories.map(c => <option value={c.id} key={c.id}>{c.name}</option>)}</select></label><label>Supplier<select name="supplier" required>{suppliers.map(s => <option value={s.id} key={s.id}>{s.name}</option>)}</select></label><label>Price<input name="price" type="number" min="1" required/></label><label>Current stock<input name="stock" type="number" min="0" required/></label><label>Minimum stock<input name="minimum" type="number" min="0" defaultValue="10" required/></label><label>Maximum stock<input name="maximum" type="number" min="1" defaultValue="200" required/></label><label>Reorder point<input name="reorder" type="number" min="0" defaultValue="20" required/></label><label>Safety stock<input name="safety" type="number" min="0" defaultValue="10" required/></label><label>Lead time (days)<input name="lead" type="number" min="0" defaultValue="3" required/></label></div><button className="primary-button">Save product</button></form>}{error && <div className="error-state">{error}</div>}<section className="panel table-panel"><div className="table-toolbar"><div className="search"><Search size={16}/><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search by name, SKU or category"/></div><span>{shown.length} products</span></div><div className="table-wrap"><table><thead><tr><th>Product</th><th>Category</th><th>Supplier</th><th>Current stock</th><th>Reorder point</th><th>Unit price</th><th>Status</th></tr></thead><tbody>{shown.map(p => <tr key={p.id}><td><strong>{p.name}</strong><small>{p.sku}</small></td><td>{p.category_name}</td><td>{p.supplier_name}</td><td className={p.current_stock <= p.reorder_point ? 'danger-text' : ''}>{p.current_stock} {p.unit}</td><td>{p.reorder_point}</td><td>₹{p.price}</td><td><StatusBadge value={p.current_stock <= p.reorder_point ? 'LOW' : p.status}/></td></tr>)}</tbody></table></div></section></>
}
